# Veritas — interface web (navegador)

Ferramenta autónoma em **um ficheiro HTML** (Tailwind via CDN). Não depende do pacote Python.

| Ficheiro | Notas |
|----------|--------|
| `payload-deobfuscator.html` | Página principal referida no README do repositório. |
| `aplicativo.html` | Variante adicional da mesma família (se existir em `web/`). |

A aplicação **desktop** (PySide6 / Qt) está em [`../ps-deobfuscator/`](../ps-deobfuscator/).
